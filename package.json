{
  "name": "snake-autonomus",
  "private": true,
  "scripts": {
    "build": "node scripts/build-browser-bundle.js",
    "check": "node --check script.js && node --check scripts/benchmark-simulation.js",
    "bench": "node scripts/benchmark-simulation.js 1024 4000",
    "dev:cf": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
